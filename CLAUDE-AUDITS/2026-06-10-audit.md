# Audit Log — 2026-06-10

## 00:00 — Project scaffolding: CLAUDE.md and audit directory

**Prompt:** Alright let's start an experiment. First off this needs to be a repo friendly, auditable experiment. So let's get a CLAUDE.MD file going. We need to track every prompt I give that results in a claude modification to files in the projects. D:\Sandbox\Chaincloud-Playground\CLAUDE-AUDITS should contain the audit files. It should break down by day using the yyyy-MM-dd-audit.md filename format.

**Files modified:**
- `CLAUDE.md` — created; project description and auditing rules for all future Claude turns
- `CLAUDE-AUDITS/2026-06-10-audit.md` — created; this file, the first audit entry

---

## 17:30 — Added consultation log system to CLAUDE.md

**Prompt:** Next I want to audit consultations. Whenever we collaborate and I choose one of your decisions we need to keep a trail of when those occur. D:\Sandbox\Chaincloud-Playground\CLAUDE-CONSULTATIONS same format

**Files modified:**
- `CLAUDE.md` — added Consultation Log section with format, entry template, and rules
- `CLAUDE-CONSULTATIONS/2026-06-10-consultations.md` — created; first consultation entry logged

---

## 17:42 — Spec-first scaffolding: project structure, OpenAPI contracts, README

**Prompt:** I'm *guessing* without researching, most CVE's in these packages we are going to stress test are likely at the network or client call level. SO the domain is going to be: Weather API and Spotify API. Weather APIs are going to talk to a Spotify API and create dynamic playlists. We need to create a crude UI to embed the Spotify controls and Weather feeds.

**Files modified:**
- `specs/public-api.yaml` — created; OpenAPI 3.1.0 contract for the .NET service (weather endpoint, playlist generate/get, health)
- `specs/playlist-engine.yaml` — created; OpenAPI 3.1.0 contract for the Go service (playlist generate, recipe-only derive, health); Spotify noted as primary outbound HTTP CVE surface
- `README.MD` — updated; architecture diagram, image comparison table, local setup, stress test instructions, credential guidance
- `.env.example` — created; placeholder for all four required credentials
- `CLAUDE-CONSULTATIONS/2026-06-10-consultations.md` — updated; Spotify auth and weather provider decisions logged

---

## 17:52 — CVE advisory research and IMAGE-DETAILS files created

**Prompt:** The next step is actually evaluating what CVEs we need to stress test as part of the specifications. D:\Sandbox\Chaincloud-Playground\IMAGE-DETAILS. Let's create new folders for each image. They should contain a .MD file of their Advisories based on what we find in the web page. Let me know if you need me to pull anything for you.

**Files modified:**
- `IMAGE-DETAILS/go/advisories.md` — created; 46 unique CVEs parsed from raw HTML fragment; openssl/glibc/binutils(under investigation) breakdown; experiment relevance analysis
- `IMAGE-DETAILS/dotnet-runtime/advisories.md` — created; 84 unique CVEs; highlights CVE-2026-21218 as the only .NET-runtime-native CVE; openssl/glibc TLS relevance
- `IMAGE-DETAILS/dotnet-sdk/advisories.md` — created; 85 unique CVEs; clarified as build-stage image; notes expat/brotli packages unique to SDK

---

## 17:54 — Added Research References table to CLAUDE.md

**Prompt:** We can scrape what we need from https://nvd.nist.gov/vuln/detail/CVE-2026-21218 and whatever CVE we stress test. Let's add the root CVE url path this to the CLAUDE.MD file so we can do joint research together.

**Files modified:**
- `CLAUDE.md` — added Research References table with NVD CVE detail URL pattern, Chainguard advisory URL pattern, and Chainguard image advisories URL pattern

---

## 17:57 — specs/README.md created; workshop open questions identified

**Prompt:** Let's continue with our spec buildout. What I've noticed is the tighter that is the cleaner our implementation. Let's start a workshop session.

**Files modified:**
- `specs/README.md` — created; documents service call flow, stress test targets per CVE surface, auth rationale, service discovery approach, and 4 open questions requiring resolution before scaffolding

---

## 18:05 — Spec v0.2 workshop: headers, experiment tracking, decisions resolved

**Prompt:** Q1: I agree. Let's cut it for now. Q2: Yes passthrough, but we should add headers we can inspect. API-SOURCE="GO-API-PASSTHROUGH" and we should make our own CorrelationID that spawns from the source. Q3: Always create new. Q4: Skip for now. In addition, we need to track what CVE's we are purposefully testing. So we need to encase each experiment in a single payload with a reference back to the experiment details.

**Files modified:**
- `specs/public-api.yaml` — v0.2; removed GET /playlist/{id}; added X-Correlation-Id, X-Experiment-Id, X-Api-Source headers; added experiment_id to GeneratePlaylistRequest, PlaylistResponse, ProblemDetails; added 429 RateLimited response
- `specs/playlist-engine.yaml` — v0.2; added X-Correlation-Id, X-Experiment-Id headers; added experiment_id to GeneratePlaylistRequest, PlaylistResult, ProblemDetails
- `specs/README.md` — updated; resolved all 4 open questions; added Header Conventions section (Correlation ID flow, Experiment ID, API-Source); added Experiment Tracking section; added Decisions Log
- `specs/experiments/README.md` — created; experiment manifest format, naming convention (EXP-NNN), index table, immutability rule

---

## 18:09 — ROADMAP.md created; deferred CVEs scoped out of Phase 1

**Prompt:** It looks like some of these CVEs require different host setups. We may need a way to spin up multiple images. Let's avoid these CVEs for now. Let's put a road map in and push that scope out.

**Files modified:**
- `ROADMAP.md` — created; Phase 1 (network/TLS CVEs, current scope), Phase 2 (deferred: privilege escalation and build-toolchain CVEs requiring special host setup), Phase 3 (future features cut from v0.1)
- `IMAGE-DETAILS/dotnet-runtime/advisories.md` — added scope note linking to ROADMAP.md
- `IMAGE-DETAILS/dotnet-sdk/advisories.md` — added scope note linking to ROADMAP.md
- `IMAGE-DETAILS/go/advisories.md` — added scope note linking to ROADMAP.md; called out binutils "under investigation" CVEs as deferred

---

## 18:47 — CVE-2023-0286 added to Phase 2 roadmap; EXP-001 manifest created

**Prompt:** Add to the roadmap and start EXP-001

**Files modified:**
- `ROADMAP.md` — added CVE-2023-0286 to Phase 2 deferred table with full rationale (requires X509_V_FLAG_CRL_CHECK + mock CA/CRL server); added mock CA + CRL server to Phase 2 infrastructure requirements
- `specs/experiments/EXP-001.md` — created; targets CVE-2024-5535 (CRITICAL 9.1), CVE-2022-3602 (HIGH 7.5), CVE-2022-3786 (HIGH 7.5); documents CVSS profiles, call path diagram, image variants, success criteria, pre-run checklist, Spotify rate limit note
- `specs/experiments/README.md` — index updated with EXP-001 entry

---
